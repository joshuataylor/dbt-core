# In-memory: the model and its failing test run in a single invocation.
failing_test:
  target: duckdb_mem
  outputs:
    duckdb_mem:
      type: duckdb
      path: ':memory:'
      threads: 1
      schema: main
